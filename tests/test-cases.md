# 测试用例：用户登录系统

## 任务 ID
TASK-20260314-001

---

## 功能测试 - 注册流程

### TC-001: 用户注册 - 正常流程
**前置条件：** 无
**测试数据：**
- 邮箱：testuser@example.com
- 密码：Password123!

**步骤：**
1. 访问 /register 页面
2. 输入有效邮箱地址
3. 输入符合要求的密码（6-20 位）
4. 点击"注册"按钮

**预期结果：**
- 注册成功，返回 201 状态码
- 响应包含用户信息和 JWT Token
- 自动跳转到登录页或仪表盘页
- Token 已存储到 localStorage

---

### TC-002: 用户注册 - 邮箱重复
**前置条件：** 已存在用户 testuser@example.com
**测试数据：**
- 邮箱：testuser@example.com（已存在）
- 密码：Password123!

**步骤：**
1. 访问 /register 页面
2. 输入已存在的邮箱地址
3. 输入密码
4. 点击"注册"按钮

**预期结果：**
- 注册失败，返回 409 Conflict 状态码
- 显示错误提示"该邮箱已被注册"
- 不创建重复用户

---

### TC-003: 用户注册 - 弱密码（长度不足）
**前置条件：** 无
**测试数据：**
- 邮箱：newuser@example.com
- 密码：12345（少于 6 位）

**步骤：**
1. 访问 /register 页面
2. 输入有效邮箱地址
3. 输入少于 6 位的密码
4. 点击"注册"按钮

**预期结果：**
- 前端验证拦截，显示"密码长度至少 6 位"
- 不发送 API 请求

---

### TC-004: 用户注册 - 无效邮箱格式
**前置条件：** 无
**测试数据：**
- 邮箱：invalid-email（无效格式）
- 密码：Password123!

**步骤：**
1. 访问 /register 页面
2. 输入无效格式的邮箱
3. 输入密码
4. 点击"注册"按钮

**预期结果：**
- 前端验证拦截，显示"请输入有效的邮箱地址"
- 不发送 API 请求

---

### TC-005: 用户注册 - 密码与确认密码不一致
**前置条件：** 无
**测试数据：**
- 邮箱：newuser@example.com
- 密码：Password123!
- 确认密码：Password456!

**步骤：**
1. 访问 /register 页面
2. 输入有效邮箱地址
3. 输入密码
4. 输入不一致的确认密码
5. 点击"注册"按钮

**预期结果：**
- 前端验证拦截，显示"两次输入的密码不一致"
- 不发送 API 请求

---

### TC-006: 用户注册 - 空值提交
**前置条件：** 无
**测试数据：**
- 邮箱：（空）
- 密码：（空）

**步骤：**
1. 访问 /register 页面
2. 不输入任何内容
3. 点击"注册"按钮

**预期结果：**
- 前端验证拦截，显示"邮箱不能为空"、"密码不能为空"
- 不发送 API 请求

---

## 功能测试 - 登录流程

### TC-007: 用户登录 - 正确密码
**前置条件：** 已存在用户 testuser@example.com / Password123!
**测试数据：**
- 邮箱：testuser@example.com
- 密码：Password123!

**步骤：**
1. 访问 /login 页面
2. 输入正确的邮箱地址
3. 输入正确的密码
4. 点击"登录"按钮

**预期结果：**
- 登录成功，返回 200 状态码
- 响应包含用户信息和 JWT Token
- 跳转到仪表盘页
- Token 已存储到 localStorage
- 后续请求自动携带 Authorization header

---

### TC-008: 用户登录 - 错误密码
**前置条件：** 已存在用户 testuser@example.com
**测试数据：**
- 邮箱：testuser@example.com
- 密码：WrongPassword!

**步骤：**
1. 访问 /login 页面
2. 输入正确的邮箱地址
3. 输入错误的密码
4. 点击"登录"按钮

**预期结果：**
- 登录失败，返回 401 Unauthorized 状态码
- 显示错误提示"邮箱或密码错误"（不泄露账号是否存在）
- Token 未生成

---

### TC-009: 用户登录 - 账号不存在
**前置条件：** 无
**测试数据：**
- 邮箱：nonexistent@example.com
- 密码：Password123!

**步骤：**
1. 访问 /login 页面
2. 输入不存在的邮箱地址
3. 输入任意密码
4. 点击"登录"按钮

**预期结果：**
- 登录失败，返回 401 Unauthorized 状态码
- 显示错误提示"邮箱或密码错误"（与错误密码提示一致）
- 不泄露账号是否存在

---

### TC-010: 用户登录 - 空值提交
**前置条件：** 无
**测试数据：**
- 邮箱：（空）
- 密码：（空）

**步骤：**
1. 访问 /login 页面
2. 不输入任何内容
3. 点击"登录"按钮

**预期结果：**
- 前端验证拦截，显示"邮箱不能为空"、"密码不能为空"
- 不发送 API 请求

---

### TC-011: 用户登录 - 无效邮箱格式
**前置条件：** 无
**测试数据：**
- 邮箱：invalid-email
- 密码：Password123!

**步骤：**
1. 访问 /login 页面
2. 输入无效格式的邮箱
3. 输入密码
4. 点击"登录"按钮

**预期结果：**
- 前端验证拦截，显示"请输入有效的邮箱地址"
- 不发送 API 请求

---

## 功能测试 - Token 处理

### TC-012: Token 验证 - 有效 Token 访问受保护路由
**前置条件：** 用户已登录，持有有效 Token
**测试数据：**
- Token: 有效的 JWT Token（未过期）

**步骤：**
1. 用户已登录
2. 访问受保护路由 /dashboard
3. 检查请求头

**预期结果：**
- 请求自动携带 Authorization: Bearer <token>
- 后端验证通过，返回 200 OK
- 页面正常加载

---

### TC-013: Token 过期处理
**前置条件：** 用户 Token 已过期（超过 2 小时）
**测试数据：**
- Token: 已过期的 JWT Token

**步骤：**
1. 用户登录，等待 Token 过期（或手动修改过期时间）
2. 刷新页面或访问受保护路由
3. 观察系统行为

**预期结果：**
- 后端返回 401 Unauthorized
- 前端 Axios 拦截器捕获 401 错误
- 自动清除 localStorage 中的 Token
- 自动跳转到 /login 页面

---

### TC-014: 无效 Token 处理
**前置条件：** 无
**测试数据：**
- Token: 伪造或篡改的 JWT Token

**步骤：**
1. 手动在 localStorage 中设置无效 Token
2. 刷新页面
3. 尝试访问受保护路由

**预期结果：**
- 后端验证失败，返回 401 Unauthorized
- 前端清除无效 Token
- 跳转到登录页

---

### TC-015: Token 刷新机制（可选）
**前置条件：** 系统实现了 refresh token 机制
**测试数据：**
- Access Token: 已过期
- Refresh Token: 有效

**步骤：**
1. Access Token 过期
2. 尝试访问受保护路由
3. 观察是否自动刷新

**预期结果：**
- 自动使用 Refresh Token 获取新的 Access Token
- 请求重试成功
- 用户无感知

---

## 功能测试 - 退出登录

### TC-016: 退出登录 - 正常流程
**前置条件：** 用户已登录
**测试数据：** 无

**步骤：**
1. 用户已登录
2. 点击"退出登录"按钮
3. 观察系统行为

**预期结果：**
- 发送 POST /api/auth/logout 请求
- 返回 200 OK
- 清除 localStorage 中的 Token
- 清除 authStore 中的用户状态
- 跳转到 /login 页面
- 无法访问受保护路由

---

### TC-017: 退出登录后访问受保护路由
**前置条件：** 用户已退出登录
**测试数据：** 无

**步骤：**
1. 用户退出登录
2. 手动访问 /dashboard  URL
3. 观察系统行为

**预期结果：**
- 检测到未登录状态
- 重定向到 /login 页面
- 不显示受保护内容

---

## 安全测试

### TC-018: SQL 注入测试 - 登录接口
**前置条件：** 无
**测试数据：**
- 邮箱：`admin@example.com' OR '1'='1`
- 密码：`anything' OR '1'='1`

**步骤：**
1. 访问 /login 页面
2. 输入 SQL 注入 payload 作为邮箱
3. 输入 SQL 注入 payload 作为密码
4. 点击"登录"按钮

**预期结果：**
- 登录失败，返回 401
- 后端日志无 SQL 错误
- 数据库无异常查询
- 使用参数化查询，注入无效

---

### TC-019: SQL 注入测试 - 注册接口
**前置条件：** 无
**测试数据：**
- 邮箱：`test@example.com'); DROP TABLE users; --`
- 密码：`password123`

**步骤：**
1. 访问 /register 页面
2. 输入 SQL 注入 payload 作为邮箱
3. 输入密码
4. 点击"注册"按钮

**预期结果：**
- 注册失败（邮箱格式验证或数据库约束）
- 数据库表未被删除
- 使用参数化查询，注入无效

---

### TC-020: XSS 测试 - 输入转义
**前置条件：** 无
**测试数据：**
- 邮箱：`<script>alert('XSS')</script>@example.com`
- 密码：`<img src=x onerror=alert('XSS')>`

**步骤：**
1. 尝试在邮箱/密码字段输入 XSS payload
2. 提交表单
3. 检查页面渲染

**预期结果：**
- 前端验证拦截无效邮箱格式
- 后端存储时正确转义特殊字符
- 页面渲染时不执行脚本
- 使用 React 自动转义，XSS 无效

---

### TC-021: CSRF 测试 - 同源检测
**前置条件：** 用户已登录
**测试数据：** 无

**步骤：**
1. 用户登录获取 Token
2. 从其他域名发起请求到 /api/auth/logout
3. 观察请求是否被处理

**预期结果：**
- 跨域请求被浏览器 CORS 策略拦截
- 或后端验证 Origin/Referer 头
- 请求失败，返回 403 Forbidden
- 同源检测生效

---

### TC-022: 暴力破解防护测试
**前置条件：** 无
**测试数据：**
- 邮箱：testuser@example.com
- 密码：尝试多个错误密码

**步骤：**
1. 使用同一账号连续登录失败 6 次（超过 5 次/分钟限制）
2. 第 7 次尝试登录（即使密码正确）
3. 观察系统行为

**预期结果：**
- 前 5 次返回 401 Unauthorized
- 第 6 次起返回 429 Too Many Requests
- 速率限制生效，防止暴力破解
- 等待 1 分钟后限制解除

---

### TC-023: 密码加密存储验证
**前置条件：** 已注册用户
**测试数据：** 无

**步骤：**
1. 查询数据库 users 表
2. 检查 password_hash 字段

**预期结果：**
- 密码以 bcrypt 哈希形式存储
- 不是明文密码
- 包含盐值（bcrypt 自动处理）
- 哈希格式：`$2a$12$...`

---

### TC-024: HTTPS 传输验证
**前置条件：** 生产环境部署
**测试数据：** 无

**步骤：**
1. 在生产环境访问登录页面
2. 检查 URL 协议
3. 使用浏览器开发者工具检查网络请求

**预期结果：**
- URL 使用 https:// 协议
- 所有 API 请求使用 HTTPS
- 无混合内容警告
- Cookie 设置 Secure 标志（如使用 Cookie）

---

## 兼容性测试

### TC-025: 浏览器兼容性 - Chrome
**前置条件：** 无
**测试环境：** Chrome 最新版本

**步骤：**
1. 使用 Chrome 访问登录页面
2. 完成注册流程
3. 完成登录流程
4. 访问受保护页面
5. 退出登录

**预期结果：**
- 所有功能正常工作
- 页面渲染正常
- 无控制台错误

---

### TC-026: 浏览器兼容性 - Firefox
**前置条件：** 无
**测试环境：** Firefox 最新版本

**步骤：**
1. 使用 Firefox 访问登录页面
2. 完成注册流程
3. 完成登录流程
4. 访问受保护页面
5. 退出登录

**预期结果：**
- 所有功能正常工作
- 页面渲染正常
- 无控制台错误

---

### TC-027: 浏览器兼容性 - Safari
**前置条件：** 无
**测试环境：** Safari 最新版本（macOS/iOS）

**步骤：**
1. 使用 Safari 访问登录页面
2. 完成注册流程
3. 完成登录流程
4. 访问受保护页面
5. 退出登录

**预期结果：**
- 所有功能正常工作
- 页面渲染正常
- 无控制台错误

---

### TC-028: 浏览器兼容性 - Edge
**前置条件：** 无
**测试环境：** Edge 最新版本

**步骤：**
1. 使用 Edge 访问登录页面
2. 完成注册流程
3. 完成登录流程
4. 访问受保护页面
5. 退出登录

**预期结果：**
- 所有功能正常工作
- 页面渲染正常
- 无控制台错误

---

### TC-029: 移动端适配 - iOS Safari
**前置条件：** 无
**测试环境：** iOS Safari（iPhone）

**步骤：**
1. 使用 iPhone Safari 访问登录页面
2. 检查页面布局
3. 完成注册/登录流程
4. 检查表单输入体验

**预期结果：**
- 页面响应式布局正常
- 表单元素大小适合触摸
- 键盘弹出正常
- 无水平滚动条
- 所有功能正常工作

---

### TC-030: 移动端适配 - Android Chrome
**前置条件：** 无
**测试环境：** Android Chrome

**步骤：**
1. 使用 Android Chrome 访问登录页面
2. 检查页面布局
3. 完成注册/登录流程
4. 检查表单输入体验

**预期结果：**
- 页面响应式布局正常
- 表单元素大小适合触摸
- 键盘弹出正常
- 无水平滚动条
- 所有功能正常工作

---

## 性能测试

### TC-031: 登录接口响应时间
**前置条件：** 后端服务正常运行
**测试数据：** 有效登录凭据

**步骤：**
1. 使用 Postman 或自动化脚本发送登录请求
2. 记录响应时间
3. 重复 100 次，计算 P95

**预期结果：**
- P95 响应时间 < 500ms
- 无超时错误

---

### TC-032: 并发登录测试
**前置条件：** 后端服务正常运行
**测试数据：** 多个有效账号

**步骤：**
1. 使用 10 个并发用户同时登录
2. 观察系统响应
3. 检查错误率

**预期结果：**
- 所有请求成功处理
- 无 5xx 错误
- 响应时间在可接受范围内

---

## 测试数据清单

### 有效测试数据
| 类型 | 数据 | 用途 |
|------|------|------|
| 邮箱 | testuser@example.com | 正常注册/登录 |
| 邮箱 | admin@example.com | 管理员账号测试 |
| 密码 | Password123! | 符合要求的密码 |
| 密码 | SecurePass666 | 符合要求的密码 |

### 边界测试数据
| 类型 | 数据 | 用途 |
|------|------|------|
| 邮箱 | a@b.co | 最短有效邮箱 |
| 邮箱 | very.long.email.address@very.long.domain.name.example.com | 长邮箱 |
| 密码 | 123456 | 最小长度密码（6 位） |
| 密码 | 12345678901234567890 | 最大长度密码（20 位） |

### 异常测试数据
| 类型 | 数据 | 用途 |
|------|------|------|
| 邮箱 | invalid-email | 无效格式 |
| 邮箱 | @example.com | 缺少用户名 |
| 邮箱 | user@ | 缺少域名 |
| 邮箱 | user@.com | 域名无效 |
| 密码 | 12345 | 长度不足 |
| 密码 | （空） | 空密码 |
| 邮箱 | existing@example.com | 已存在邮箱 |

### 安全测试数据
| 类型 | 数据 | 用途 |
|------|------|------|
| SQL 注入 | `' OR '1'='1` | SQL 注入测试 |
| SQL 注入 | `'; DROP TABLE users; --` | 破坏性注入测试 |
| XSS | `<script>alert('XSS')</script>` | XSS 测试 |
| XSS | `<img src=x onerror=alert('XSS')>` | 事件处理器 XSS |

---

## 测试环境检查清单

### 后端环境
- [ ] PostgreSQL 数据库运行正常
- [ ] Prisma migration 已执行
- [ ] 后端服务启动（默认端口 3000）
- [ ] .env 配置正确（JWT_SECRET、数据库连接等）
- [ ] 速率限制中间件已启用

### 前端环境
- [ ] Node.js 环境就绪
- [ ] 前端依赖已安装（npm install）
- [ ] 前端服务启动（默认端口 5173）
- [ ] API 基础 URL 配置正确
- [ ] localStorage 可用

### 测试工具
- [ ] Postman/Insomnia 已安装
- [ ] 浏览器开发者工具可用
- [ ] 网络抓包工具（可选）
- [ ] 多浏览器环境（Chrome、Firefox、Safari、Edge）
- [ ] 移动端测试设备或模拟器

### 测试账号准备
- [ ] 创建测试账号 testuser@example.com
- [ ] 确认测试账号密码已知
- [ ] 准备多个测试账号用于并发测试

---

*测试用例版本：v1.0*
*创建时间：2026-03-14*
*创建者：QA Engineer (小爪)*
